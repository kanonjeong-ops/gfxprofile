# 설계 개정 10판 초안 (2판) — UI 실기 결함 D1~D6 수정 (DESIGN-AMENDMENT-R10-DRAFT-2026-08-13)

> **지위: 채택 확정(2026-08-13) — 반증 1패스 반영 2판, 개정 10판으로 정본 반영 완료. repo 편입본.**
> **★ 2026-09-02 부기 — 이 문서는 2026-08-13 채택 시점의 개정안 기록이다.** F절 ⓑ(비활성 Primary 면)와 §16-⑮ ⓗ 판정은 2026-09-02 실기에서 **철회**됐다(면이 작은 버튼으로 오독) — 현행 정본은 `DESIGN-UX-2026-08-11.md` §4-C 철회 조항 · §19 19판 보정 3. 이 문서의 현재형 문장은 그 시점의 사실로 읽는다 — 소급 수정하지 않는다.
> 참모(Fable) 작성, 2026-08-13. 진단 정본 = `DIAG-UI-FIELD-2026-08-12.md`(단, §2 일부는 실측으로 정정 — 본문 A-1).
> 구조: 개정 절마다 「현행 조문 인용 → 개정 조문 전문 → 근거 → 파급」. 이 초안만 읽고 구현 워커가
> §10-A 표 갱신과 코드 수정에 착수할 수 있어야 한다.

## 0-0. 초안 2판 개정 이력 (2026-08-13 — 독립 반증 1패스(Opus, 샌드박스 주입+회귀 실행형) 세션 처분 반영)

검토자 종합 = "전부 국소 수정으로 닫히는 결함 — 설계 방향 붕괴 없음". 교정 전량 적용 시뮬레이션에서
회귀 관련 18종 PASS 실측. 2판 반영 전량:

1. **결정 4건 확정 편입**: R10-1 = **B안**(사용자) · R10-2 = ⓑ 권고 수준(세션) · R10-3 = 채택(세션) ·
   R10-4 = 채택·E절 본문 편입(세션). §0 결정 표를 확정 기록으로 전환.
2. **D절**: `RESET_CONFIRM_TYPE` 전수 표 누락 정정(D-1 추가·문구 개정 — 게이트 무예외 원칙 유지) ·
   실물 키 수 233→**231** 정정(E-3 연동 229) · D-4 병기 허용쌍에 `(으)로` 추가 ·
   TOTAL_FIXED의 PINNED=0 전제 명기.
3. **A절(최심각)**: 래퍼 코드 확정형 교체(`ComponentProps<typeof DialogButton>` — grep ① 위반 회피 /
   **children 구조분해+JSX 자식 전달** — probe_kit의 children prop 스킵 탓 회귀 4종 파괴 실측 회피) ·
   2항↔A-4 모순 해소(폴백 오버레이 푸터 2버튼 = 직접 사용 예외 유지) · 세로 스택 실물 6곳(4·1·1)
   정정 + 가로 행 제외 명기 · ref 미투과 주기 · 288.3px 출처 정정.
4. **C절**: `CARD_STYLE.marginBottom:4px` 인용 보완 + 카드 간격 8px과의 처분 조문 ·
   `APPLY_ALL_STOPPED`는 "승계"가 아니라 **9판 조문 정오**로 분리 · 시각 표기 변경을 "개정"으로 명시.
5. **E절**: `test_confirm_keys` 음성 대조군 "9개다"→"7개다" 하드코딩 갱신 지시 · 낡은 주석 2곳 파급
   (confirmSpecs:236-237 필수 / main.py:574 — 재량 처분 = **갱신**, 백엔드 주석 1건 예외 명기) ·
   **설정 화면 중복(RESET_ZONE_BODY↔RESET_HINT) 편입**(세션 채택) · R10-4(MANAGE_KEEP_CONFIG 회색
   강등) 본문 편입 · §10-B 정본 "245→227" 문장 자체의 낡음(실물 231)을 10판 정정 목록에 등재.
6. **G절**: 영향표 정정(test_restore_ui 추가 · test_discover_ui 정규식 3식 · probe_kit 무수정 근거 ·
   test_frontend_e1은 C절 아닌 **B절** 소속 + 통과 근거) · 잠금 집계 갱신.
7. **잠금 집계 최종**: 값 명시 신설 행이 5개가 되어(D5 계열 4 + E절 1) **LOCKED 28→33 ·
   TOTAL_FIXED=33**. 통지의 "31 확정"은 지적 1(RESET_CONFIRM_TYPE)·지적 14(RESET_ZONE_BODY) 신설
   행 **이전** 값이다 — §10-A 규칙("값을 명시한 키는 전부 잠금, 면제 목록 금지")상 두 행도 잠근다.
   이 산정 차이는 세션 확인 요망(별도 이견 아님 — 규칙 기계 적용의 결과).

## 0. 판수 확인 보고 + 결정 처분 기록 (2판 — 전건 확정)

**판수**: 정본 파일 제목줄은 "개정 8판"인데 §19 개정 이력에는 **9판(2026-08-12, P16 게이트 조건 C1~C7 반영)이 실재**하고,
본문 곳곳에 "9판 확정(P16 C1)"류 표기가 살아 있다. **실물 최신판 = 9판, 제목줄만 낡았다**(9판 반영 때 제목줄 갱신 누락).
이 초안은 9판을 기준으로 하며, 채택 시 **제목줄을 "개정 10판"으로 정정**한다(8→9 누락도 이때 함께 흡수).

**결정 확정(2판 — 전건 처분 완료, 대기 0)**:
| # | 사안 | 확정(2026-08-13) |
|---|---|---|
| R10-1 | 상태박스 A안/B안 | **B안**(사용자) — C-1이 정본 조문, C-3은 결정 기록으로 축약 |
| R10-2 | 신규 소견 ⓑ 조문화 수준 | **권고 수준**(세션 — 참모 추천 채택, F절) |
| R10-3 | `APPLY_ONE_OK` 문형 개정 | **채택**(세션 — 검토자가 신 값 3종 주입 시 관련 게이트 전부 PASS 실측) |
| R10-4 | `MANAGE_KEEP_CONFIG` 주황→회색(META) 강등 | **채택**(세션 — 안심 정보가 경고 옷을 입은 것, 검토자 스샷 확인. E절 본문 편입) |

**불변 제약 준수 선언**: 이번 개정 전체가 **프론트 표시층 + i18n + QA**에만 닿는다.
엔진 fence 5파일 무접촉(§14-A 포함 — 이번 개정은 §14에 아무것도 더하지 않는다). 공리 0-A(재설계 금지 — 전부
기존 계약의 구체화·정오), A5·A6 준수, A8 준수(신규 색 0 — 회색/주황 2단 유지, 빨강 없음), A10 무접촉
(popupWidth류 재도입 없음), 수직 단일 축(GP#12)은 QAM 요소 배치 계약이고 카드 내부 마이크로 레이아웃
(아이콘+시각 행)은 현행 §3-B ⑤(RESULT_ROW_STYLE)에도 이미 있던 것이라 비저촉.

**절 순서**: 브리핑 개정 범위 번호와 다르게, 다른 절이 딛는 전제(폭 규약)를 A절로 앞세웠다.
대응표 — A절=범위4(§4 폭 규약) · B절=범위3(§3-A 정오) · C절=범위1(§3-B 상태박스) · D절=범위2(§10-A D5) ·
E절=범위5(§7 중복 — 확정 승격분) · F절=신규 소견 ⓐⓑ.

---

## A절. §4-G 신설 — 팝업 버튼 폭 규약 (D1: 전역 1곳)

### A-0. 현행 조문 인용

§4에는 버튼 폭 규약이 **없다**(§4-D는 스크롤·크기 — 목록 컨테이너와 입력 모달 minWidth만).
§5-A에만 행 버튼 계약이 있다:

> - `[{profile} 적용]` ×2 — **동일 `minWidth`(110px) 고정 + 마커 자리 상시 예약**(GP#2): … → 전 행 버튼 x좌표 동일.

### A-1. 사실 정정 (진단 §2를 뒤집은 실측 — 이 절의 딛는 땅)

- Steam 본체 CSS `button.DialogButton { width:100% }`. 팝업 본문 실효 폭 `.DialogContent_InnerWidth` = **604.7px**(진단의 600은 유도 추정치).
- 진단 §2의 "블록 부모 4곳만 오염, flex row 자리는 정상"은 **실측으로 반증됐다**: flex 자식이어도
  `flex-basis:auto`가 `width:100%`를 그대로 읽는다 — 목록 행의 적용 버튼(`flex:0 0 auto`)이 각각 **584.7px**,
  게임 이름 div(flex:1 1 auto·minWidth:0)는 **0px로 붕괴**(식별 불가), [내장 적용]·chevron은 모달 오른쪽 밖
  (가로 3배 오버플로). 설정 팝업 "기본 이름" 라벨·목록 헤더 토글 라벨도 같은 원리로 세로 1자/줄 붕괴.
- 세로 잘림 가설 ⓑ는 참(상세 뷰 bottom 641.9 > 575)이나 Steam 모달 스크롤러(overflowY auto)가 닿고,
  넘침의 주범은 라벨 붕괴였다 — **폭 규약이 대부분을 함께 해소한다**. A10은 무죄(재확인).
- **확인창 2종(일괄 적용·전체 초기화)의 푸터는 정상** — DialogTwoColLayout이 flex-grow:1로 반폭씩 분배
  (288.3px는 **일괄 적용 확인창** 실측값이고, 폭이 다른 초기화 확인창은 ~603px 반폭 — 어느 쪽이든
  flex-grow 분배라 `width:100%` 오염에 구조적 면역, 결론 불변). **수정 대상에서 제외 확정.**
- 따라서 수정 방향은 진단 §3의 "세로 스택 4곳 상수(STACKED_BUTTON_STYLE)"가 아니라
  **팝업 전 DialogButton에 대한 전역 폭 규약 1곳**이다(세션 기확정. 진단 §3 해당 문장은 이 절이 대체한다).

### A-2. 개정 조문 전문 — §4-G (신설)

> ### 4-G. 버튼 폭 규약 (10판 신설 — D1)
>
> **사실**: Steam 본체 CSS는 `button.DialogButton { width:100% }`를 전역 적용한다. 이것은 블록 부모만이
> 아니라 flex 자식에도 발현한다(`flex-basis:auto`가 width를 읽는다) — 인라인 `width`를 명시하지 않은
> 모든 DialogButton은 팝업 본문 폭(≈604.7px)으로 늘어난다. 미니 프로브는 이 계열을 **원리적으로 못
> 잡는다**(Steam 스타일시트가 프로브에 없다) — 판정은 실기 CDP 스타일 조회 또는 스샷뿐이다.
>
> **규약**:
> 1. **팝업 층의 모든 버튼은 `popup.tsx`의 래퍼 `PopupButton` 하나로만 그린다.** 래퍼는 `DialogButton`에
>    `width: "fit-content"`를 **호출부 style보다 나중에** 병합해 전역 오염을 차단한다(호출부는 width를
>    지정하지 않는다 — minWidth·flex·padding·색만). props는 전부 투과(preferredFocus·disabled 등).
>    ```tsx
>    // popup.tsx — 확정형(2판, 반증 실측 반영. 이 형태에서 벗어나지 말 것)
>    export function PopupButton({ style, children, ...props }: ComponentProps<typeof DialogButton>) {
>      return <DialogButton {...props} style={{ ...style, width: "fit-content" }}>{children}</DialogButton>;
>    }
>    ```
>    - 타입은 **`ComponentProps<typeof DialogButton>`**이다 — `DialogButtonProps`를 `@decky/ui`에서
>      직접 import하면 **build.sh grep ①(단일 관문) 위반**이고 `deckyui.ts`는 그 타입을 재수출하지
>      않는다(실측).
>    - **children은 구조분해해 JSX 자식으로 전달한다** — `{...props}`에 children을 남겨 prop으로
>      넘기면 `probe_kit.cjs`가 children **prop**을 스킵하고(:172) JSX 자리 children에서만 라벨을
>      읽어(:208) 전 버튼 라벨이 ""가 된다 — 회귀 4종 파괴(test_games_popup 크래시 포함) 실측.
>      JSX 자식 전달형으로 3종 복구 실측.
>    - `ref`는 투과되지 않는다(`ComponentProps`는 RefAttributes 미포함) — 현 호출부에 ref 사용 0이라
>      무해. ref가 필요해지는 날 forwardRef로 확장한다(지금 만들지 않는다).
> 2. **문 하나 강제**: `<DialogButton` JSX의 직접 사용은 `popup.tsx` 한 파일만 허용. 파일 안에서도
>    두 자리뿐이다 — ⓐ 래퍼 정의부 ⓑ **폴백 오버레이 확인창의 푸터 2버튼**(예외 사유: Steam
>    `ConfirmModal` 푸터의 flex-grow 반폭 열과 시각을 동일하게 유지해야 §4-C "두 렌더러가 같은 것을
>    그린다" 계약과 F-1이 자기 정합한다 — fit-content로 접으면 중첩/폴백의 푸터 모양이 갈린다).
>    `build.sh` grep ⑤가 파일 단위로 잠근다(`@decky/ui` import 단일 관문 grep과 동형 — build.sh:72-74
>    선례). 파일 밖 예외 목록은 두지 않는다.
> 3. **세로 스택 버튼**(설명 줄을 아래에 거느리는 동작 버튼 — 실물 6곳: 팝업 G 상세 4(ActionButton) ·
>    설정 1(`RESET_OPEN`) · 감지 1(`DISCOVER_PICK_FILE`)):
>    fit-content(1항 자동) + flex 부모에서는 `alignSelf:"flex-start"` + **내용 좌측 정렬**(`textAlign:"left"`,
>    런타임에 버튼 내부가 flex로 판명되면 `justifyContent:"flex-start"` — 실기 §16-⑮ ⓓ에서 확인).
>    설명 줄(desc)은 버튼의 가로 패딩과 같은 `paddingLeft`(공용 상수 하나)로 **라벨과 정렬 축을 공유**한다
>    — 현행의 "버튼 글자 가운데·설명 왼쪽" 축 어긋남 해소.
>    **가로 행 버튼은 대상이 아니다**(좌측 정렬·alignSelf를 강제하지 않는다): 이름 바꾸기 2(ROW_STYLE) ·
>    [다시 검색]·[모두 추가] 액션 줄 · 목록 행 버튼들 — 이들은 행 안에서 제 폭만 접으면 된다(1항).
> 4. **§5-A 정합**: 행 버튼의 "minWidth 110px 고정·전 행 x좌표 동일" 계약(GP#2)은 **불변이며 살아 있다**
>    — 584.7px로 늘어난 현 상태가 오히려 설계 위반이었다. fit-content에서도 계약이 성립하는 근거:
>    행 버튼의 내용(프로필 표시명 + 마커 자리 상시 예약)은 **전 행에서 동일**하므로 폭도 전 행 동일이다.
>    minWidth 110은 하한으로 존치. 행 버튼에 별도 maxWidth는 두지 않는다 — 상한의 문은
>    표시명 길이 상한(`PROFILE_NAME_MAX`=20, `limits.ts`) 하나다(알려진 한계: 20자 꽉 채운 표시명이면
>    두 적용 버튼이 게임 이름을 크게 압박한다 — 병적 사례로 처분, 실기 ⑥ 실측 범위).
> 5. **적용 제외**: ⓐ QAM의 `ButtonItem`(268px 폭 정상 실측 — QAM에서는 전폭이 옳다)
>    ⓑ 확인창 2종의 푸터 버튼(ConfirmModal 내부 — DialogTwoColLayout이 flex-grow로 분배, 오염 면역 실측).

### A-3. 근거

- **구조로 예외를 없애라**: 진단이 꼽은 4곳만 고치면(요소별 땜빵) 다음 버튼이 같은 함정에 빠진다.
  래퍼 + grep은 "빠뜨릴 자리" 자체를 없앤다 — E1을 텍스트 검사에서 구조(props 부재)로 옮긴 것과 같은 판단.
- fit-content를 래퍼가 **나중에** 병합하는 이유: 먼저 병합하면 호출부가 무심코 width를 덮어 문이 샌다.
  팝업 층에서 전폭 버튼이 필요한 자리는 실측상 0곳이다(전폭이 필요한 확인창 푸터는 Steam 소유라 제외 5항).
- 레퍼런스 정합: 아이콘 단독 버튼을 인라인 width+min-width로 누르는 Framegen 관용구와 같은 방향
  (증거 워커 실측 — 오염을 각 버튼이 인라인으로 누른다). 우리는 그것을 한 곳으로 모았을 뿐이다.

### A-4. 파급

| 대상 | 내용 |
|---|---|
| 코드 | `popup.tsx` `PopupButton` 신설 · `GamesPopup.tsx`(5곳)·`DiscoverPopup.tsx`(9곳)·`SettingsPopup.tsx`(2곳)의 `<DialogButton` 전량 치환 · `popup.tsx` 내부는 **뒤로 버튼 1곳만 래퍼 전환, 오버레이 푸터 2곳은 직접 유지**(A-2 2항 ⓑ 예외 — 중첩/폴백 푸터 시각 동일성) · 세로 스택 desc 들여쓰기 상수 1개 |
| 잠금 | **build.sh grep ⑤ 신설**(DialogButton 단일 관문 — §15-A build.sh 행에 추가). E1 게이트(grep ③)·A5 462건·LOCKED 28키 무접촉 |
| 검사 | `games_popup_probe`(⑧ 폭·마커 자리 검사)·`settings_popup_probe`·`popup_shell_probe` — 스타일 검사가 래퍼 통과 후에도 minWidth를 읽는지 확인(판정 로직 무변경 예상, 목 갱신만). **폭의 실판정은 프로브가 원리적으로 불가** — 스샷 표본(§16-⑮)이 정본 |
| 타 절 | §5-A(계약 존치 명문 — 4항) · §4-D(무변경 — 폭 규약은 §4-G로 분리) · 진단 §3 STACKED_BUTTON_STYLE 문장은 이 절이 대체 · §12 이식 맵 신규 파일 열에 `PopupButton` 언급 불요(popup.tsx 내) |
| 실기 | §16-⑮(F절에서 신설) ⓐ 팝업 G 목록/상세 스샷 표본 ⓑ 세로 잘림 재실측(폭 수리 후 잔존 여부 — 남으면 §4-D 420px 조정으로, 상세 뷰 구성 변경은 하지 않는다) |

---

## B절. §3-A 정오 — QAM 버튼 아이콘 관용구 (D2·D6)

### B-0. 현행 조문 인용

§3-A 1·2항:
> 변경 2가지뿐: ⓐ 라벨 키 문구(§10)+`icon` prop(§11) ⓑ 표시 보조 prop `hint?: string` 신설(M1) …

§3-A 6~8항:
> 6~8: `ButtonItem`(icon+label+description — `Item.d.ts:1-11` 공짜 슬롯). …

### B-1. 개정 조문 전문 (해당 두 문장 대체)

> - 1·2: **활성 조건 불변**(`disabled={ready===0||busy}` — E1). 변경 2가지뿐: ⓐ 라벨 키 문구(§10) +
>   **children 안 아이콘 관용구(§11 — 아래 상자)** ⓑ 표시 보조 prop `hint?: string` 신설(M1) — (이하 불변).
> - 6~8: `ButtonItem`(label+description — description 슬롯은 `Item.d.ts:1-11`). **아이콘은 children 안이다**(아래 상자).
>
> **⚠️ 10판 정오 — `ButtonItem`의 `icon` prop 사용 금지(D2 실측)**: `icon` prop은 버튼 내용이 아니라
> **Field의 라벨 슬롯**이다 — `layout="below"` + label 없음 조합에서 아이콘이 버튼 위 26px에 고아로 뜬다
> (QAM 5버튼 전부 재현). 라벨 슬롯의 부모(h=20)는 아이콘을 16×20 비정사각으로 구겼다(D6) —
> 버튼 안 svg는 16×16 정상 실측이므로 children 이동으로 D6도 함께 소멸한다(§11 조문 무변경).
> 초판~9판의 "icon+label+description 공짜 슬롯" 서술은 오류였다.
>
> **아이콘 관용구는 프로젝트에 하나다**: `<span style={ICON_SLOT_STYLE}><Icon/></span>{label}` —
> 팝업 버튼들이 이미 쓰는 그 형태를 QAM도 쓴다. `ICON_SLOT_STYLE`(inline-flex·width 1em·center·
> marginRight 4px)은 **`popup.tsx`로 승격해 export**하고(현행은 `GamesPopup.tsx:62`·`SettingsPopup.tsx:33`·
> `DiscoverPopup.tsx:50` 3파일 복제 — 한쪽만 손대는 날 아이콘 배치가 화면마다 갈린다), 소비자는
> 팝업 3종 + `index.tsx`(EntryButton) + `BulkApplyButton.tsx`다. 동종 레퍼런스의 원형(Framegen: 버튼
> children 안 flex 컨테이너 + svg 직계 + 텍스트)과 같은 메커니즘이며, span 슬롯을 유지하는 이유는
> 마커 자리 상시 예약(GP#2)과 짝을 이루는 고정 폭 자리가 우리 쪽 요구이기 때문이다.

### B-2. 근거

- D2는 **확정 결함**(스샷+DOM — 진단에서 유일하게 실측 확정이었고 이번 재실측도 일치): 수정 지점 2곳 =
  `index.tsx:351`(EntryButton) · `BulkApplyButton.tsx:48-51`.
- "아이콘 배치법을 프로젝트에 하나만"은 진단 §3 D2 방향 그대로이며, 승격 위치가 popup.tsx인 것은
  CARD_STYLE 승격 선례("소비자가 둘이 되면 공용으로")와 같은 판단. index.tsx가 popup.tsx를 import하는
  의존 방향은 P15-E(useDataDoor)에 선례가 있다.

### B-3. 파급

| 대상 | 내용 |
|---|---|
| 코드 | `index.tsx:351` EntryButton — `icon` prop 제거, children 선두에 span 관용구 · `BulkApplyButton.tsx:48-51` 동일 · 팝업 3종의 로컬 `ICON_SLOT_STYLE` 삭제 → popup.tsx import |
| E1 | **무접촉** — BulkApplyButton의 props 집합·활성 조건·running 부재 구조 불변. build.sh grep ③은 `running` 참조 위치만 보므로 통과. 이 사실을 커밋 메시지에 명기 |
| 검사 | `qam_probe.cjs`가 `props.icon`을 읽는다(:218·:309·:311) — **children 관용구 판독으로 갱신**(판정 항목 "5버튼 전부 아이콘 실재"는 유지, 읽는 자리만 변경). `test_frontend_e1`/`frontend_probe`는 **이 절(B절) 소속의 영향권이되 무수정-통과**(2판 정정 — frontend_probe.cjs:229가 `BULK_APPLY` 라벨을 textOf로 선별하는데, 아이콘 span을 children 선두에 넣어도 textOf가 요소 서브트리를 ""로 돌려 선별이 그대로 성립함을 검토자가 실측. 통과가 우연이 아니라 이 근거 때문임을 여기 남긴다) |
| 타 절 | §11 무변경(D6은 원인 소멸 — §11 아이콘 규격이 옳았고 배치가 틀렸던 것) · §3-A ⓑ hint 계약 불변 |
| 실기 | §16-⑮ ⓒ QAM 최상단 스샷 1장(5버튼 아이콘이 버튼 안·16×16) |

---

## C절. §3-B 상태박스 — 카드형 재작성 (D3·D4) 【R10-1 = B안 확정(사용자, 2026-08-13) — C-1이 정본 조문】

### C-0. 현행 조문 인용

> ### 3-B. 상태박스 계약
> **한 개의 시각 박스**. 스타일: `border:1px solid rgba(255,255,255,0.15); borderRadius:4px; padding:8px 10px;
> fontSize:12px;` 줄 간 `gap:6px`. 내용 0줄이면 미렌더.
> 내부는 **두 군**으로 나눈다(D06 — 위=지금, 아래=직전 결과. 사이 8px 여백): …(슬롯 ⓪~⑦ 표)…
> - **수명**: 과거군은 `lastSummary` 모듈 변수 승계(불변). **실패(③)도 `lastFailure` 모듈 변수로 동일 승계**(F20 …), 소거 시점 = **다음 동작 시작**.
> - **busy 동안 과거군은 투명도 0.4**(D02-ⓑ …).
> - `PROFILE_INCOMPLETE`(A6 삭제)·`NO_GAMES`(카운트 자리로)는 상태박스에 없다. 색은 2단(회색/주황) — 빨강 없음(A8).

(실물: `index.tsx` `BOX_STYLE`(:283-290)·`GROUP_STYLE`(:292)·`RESULT_ROW_STYLE`(:295)·`renderStatusBox`(:627-707).)

### C-1. 개정 조문 전문 — §3-B 전면 대체 (B안)

> ### 3-B. 상태박스 계약 (10판 — 결과 카드형)
>
> 상태박스는 **최대 2장의 결과 카드**다(위 = 현재군 카드, 아래 = 과거군 카드).
> 카드 시각은 팝업 목록 카드와 **같은 언어**(`popup.tsx`의 `CARD_STYLE` 재사용 — 실물은 `background:
> rgba(255,255,255,0.05); borderRadius:4px; padding:8px 10px; marginBottom:4px`(popup.tsx:144-149),
> 여기에 `fontSize:12px`·내부 줄 `gap:6px`).
> **카드 간격의 문은 하나다(10판)**: 상태박스 컨테이너가 `display:flex; flexDirection:column; gap:"8px"`로
> 군 간 8px를 소유하고, 카드에는 `marginBottom:0`을 **CARD_STYLE 뒤에** 병합해 목록용 4px 간격을 덮는다
> (간격 값이 두 곳에 살면 한쪽만 고치는 날 군 간격이 12px로 어긋난다).
> 9판까지의 외곽 테두리 박스(`border:1px solid rgba(255,255,255,0.15)`)는 폐기한다. 내용 0줄인 카드는
> 미렌더, 두 카드 모두 0줄이면 상태박스 전체 미렌더(승계).
>
> **[현재군 카드]** — 슬롯 ⓪~④의 조건·키·색 계약은 9판과 **바이트 동일**(무변경 승계). 각 슬롯은
> **전폭 블록 줄**이고, ③ 실패의 수신 시각은 문장 뒤 인라인 텍스트다(flex 아님 — 감김 붕괴 원리적 불가).
>
> **[과거군 카드]** — 직전 일괄 적용 결과. 구성:
> 1. **머리 행**(flex row · `justifyContent:"space-between"` · `alignItems:"center"`):
>    좌 = 성패 아이콘(문제 행 0 → `IconCheck` 회색 / 있음 → `IconWarn` 주황 — D05 승계, 16px 고정),
>    우 = **수신 시각**(당일 "14:32", 당일 아니면 "08-10 14:32" — 표기 규칙(D02·M9)은 승계,
>    **배치는 개정이다(2판 명시)**: 9판의 헤드라인 뒤 인라인 `" · 14:32"`에서 머리 행 우측 정렬로
>    옮기고 구분자 "·"를 제거한다. `flexShrink:0`, 회색).
>    **가변 텍스트는 이 행에 없다** — 고정폭 둘 사이는 빈 공간이다.
> 2. **헤드라인 줄**: `APPLY_SUMMARY`/`APPLY_ALL_UNCHANGED`/`APPLY_ALL_STOPPED` — 굵게,
>    전폭 블록, 자연 줄바꿈(ellipsis 금지 — 수를 자르면 화면이 거짓을 말한다).
>    **⚠ 10판 정오(2판 분리)**: `APPLY_ALL_STOPPED` 편입은 "승계"가 아니라 **9판 §3-B ⑤ 조문의
>    정오**다 — 9판 ⑤는 헤드라인 키를 2종만 명시했는데 실물(index.tsx:190)은 P15-E부터 3종이고
>    §10-C(:708)에 이미 등재돼 있었다(문서 쪽이 낡았던 자리, "되돌리지 말고 문서를 고친다").
> 3. **사유 줄**(⑥ 승계): `APPLY_PROBLEM_REFUSED`+`APPLY_PROBLEM_SPECIFIC` 묶음 — 주황, 전폭 블록.
> 4. **체크인 고지**(⑦ 승계): `CHECKIN_MANY` — 회색, 전폭 블록. 0건이면 줄 없음.
>
> - **수명 계약 승계(무변경)**: 과거군 = `lastSummary` 모듈 변수 · 실패 = `lastFailure` 동일 승계(F20) ·
>   소거 = 다음 동작 시작. **busy 동안 과거군 카드 전체 투명도 0.4**(D02-ⓑ — 카드 단위 적용).
> - **폭 규약(D4 원천 봉쇄)**: 상태박스 안에서 **고정폭 요소(아이콘·시각)와 가변 텍스트를 같은 flex 행에
>   두지 않는다.** 가변 텍스트는 항상 전폭 블록 줄이다. (9판 ⑤의 아이콘+헤드라인+시각 한 줄 배치와
>   그 실물 `RESULT_ROW_STYLE`은 폐기 — 268px에서 헤드라인 `minWidth:auto`·시각 `flexShrink` 부재로
>   3항목이 전부 감기는 붕괴가 실측됐다(D4). 이 규약 아래에서는 그 붕괴가 **재발할 형태 자체가 없다**.)
> - `PROFILE_INCOMPLETE`·`NO_GAMES` 부재, 색 2단(회색/주황·빨강 없음 A8) — 전부 승계.
> - **정직성 조항(승계·명문화)**: 현재 데이터는 로그가 아니다(summary 1건+failure 1건, 이력 없음) —
>   카드형은 "기록이 더 있다"는 약속을 하지 않는 형태다. 진짜 로그 이력(C안)은 유지보수 모드와
>   충돌해 후순위 확정(재상정 없음).

### C-2. 근거

- **레퍼런스 관습**: Steam 설정·LSFG·SimpleDeckyTDP·Framegen 캡처 전반에 "투명 배경+테두리 상자"가
  부재하고 묶음은 전부 채운 배경이다(증거 워커 실측) — 현행 테두리 박스가 "로그창도 카드도 아닌
  테두리 친 문단"(D3, 사용자 접수 그대로)으로 읽힌 원인.
- **시각 언어 통일**: CARD_STYLE 정본 1곳 재사용 — 앱 전체에서 "묶인 결과물"의 형태가 하나가 된다.
- **D4의 구조적 소멸**: A안류의 "flex 규약 수리"는 규약을 지키는 동안만 안전하다. B안의 형태
  (고정폭 행/전폭 텍스트 분리)는 어길 자리가 없다.
- QAM 268px 실측 기준 검증: 머리 행이 아이콘 16px+시각 ~40px만 쓰므로 헤드라인은 전폭(~248px 유효)
  ≈ 12px 글꼴 한글 20자/줄 — "내장 프로필 적용 — 전체 10개 중 8개 변경"(D절 신 문구)이 2줄로 완결.
  9판 실물(스샷 qam-1952)의 "…8개 변/경" 낱자 감김·시각 고아가 사라진다.

### C-3. A안/B안 결정 기록 — 결정 완료(B안, 사용자 2026-08-13)

초안 1판이 A안(현 테두리 박스 + 어두운 배경·좌측 색 띠·군 구분선·동일 폭 규약)과 B안의 268px 비교
재료를 붙여 상정했고, **사용자가 B안을 확정**했다. 판단 재료 요지만 남긴다 — A안은 형태 문법이
그대로라 "테두리 문단"이라는 D3의 인상이 남을 수 있고 QAM 시각 계열과 이질적, B안은 카드 정본
1곳 재사용으로 시각 통일 + D4가 규약이 아니라 형태에서 소멸(비용은 renderStatusBox DOM 재배열이나
수명·조건 로직 무변경 — 표시층 한정). 참모·진단 분석관 권장과 일치. **재상정 없음.**

### C-4. 파급

| 대상 | 내용 |
|---|---|
| 코드 | `index.tsx` — `BOX_STYLE`·`GROUP_STYLE`·`RESULT_ROW_STYLE` 폐기 → CARD_STYLE import + 카드 2장 구조. `renderStatusBox` 슬롯 조건·수명 로직 무변경(표시 형태만) |
| 잠금 | LOCKED 28키 무접촉(문구 무변경 — 문구는 D절 소관) · A5 무접촉 · E1: 슬롯 ②의 `e1-display-only` 마커 줄(`index.tsx:599-600`)은 **이동만, 문법 불변** — build.sh grep ③ 마커 동반 확인 |
| 검사 | §15-B ⑤(상태박스 프로브): **`qam_probe.cjs`만**이다 — :183이 상태박스를 border 스타일 셀렉터로 찾으므로 **카드 셀렉터로 갱신 필수**(테두리 폐기 후 그대로면 "박스 없음" 오판). 슬롯 조건·군 분리·시각·busy 투명도 판정은 유지(투명도 적용 대상 = 과거군 카드). `frontend_probe`는 상태박스 판독 0건 — C절 무영향(2판 정정, B절 소속) |
| 타 절 | §3-B를 참조하는 §3-D(조건 재사용)·§5-E(⑦ 고지)·§16-⑪(실기 항목) — 전부 계약 무변경이라 자동 정합. §11(IconCheck/IconWarn 사용처 서술) 무변경 |
| 실기 | §16-⑪ 그대로 유효 + §16-⑮ ⓔ 카드형 렌더 스샷(268px에서 헤드라인 줄바꿈 확인) |

---

## D절. §10-A 값 개정 + §10-E 신설 — 치환자 조사 (D5)

### D-0. 현행 조문 인용

§10-A에 해당 행이 **없다**(이번에 신설). 관련 현행: `APPLY_SUMMARY`는 어느 표에도 없고,
`SAVE_GUIDE`는 §10-C 신설 표에 "§5-B 문구 3종"으로만 있으며 §5-B 다이어그램이 잠정 문구를 보여준다:
> 프로필 저장: eGPU를 연결한 상태에서 게임 설정을 맞춘 뒤 [eGPU 프로필 저장], …

실물 `ko.json`(HEAD `f9a30ce`):
> `APPLY_SUMMARY`: "{profile}로 전환 — 전체 {total}개 중 {applied}개 변경" (:28)
> `SAVE_GUIDE`: "{dock}를 연결한 상태에서 게임의 그래픽 옵션을 맞춘 뒤 [{dock} 프로필 저장]을, {internal}으로 쓸 그래픽 옵션을 맞춘 뒤 [{internal} 프로필 저장]을 누릅니다." (:213)
> `APPLY_ONE_OK`: "{profile}을(를) 적용했습니다" (:36)

### D-1. `{placeholder}` 뒤 조사 전수 점검 (ko.json **231키**(2판 정정 — en 동일 231) 실물 전량 — 설계 표 아닌 파일 기준)

| 키 | 실물 | 판정 |
|---|---|---|
| `APPLY_SUMMARY` | `{profile}로 전환` | **결함(D5 실물 그 자리)** — 이형태 단독 "로"("내장로" 실기 재현). **개정 D-2 ①** |
| `SAVE_GUIDE` | `{dock}를` · `{internal}으로` | **결함(잠복)** — 기본 표시명에서는 우연히 맞으나 F11 커스텀 이름("독"·"iGPU"류)에서 틀린다. **개정 D-2 ②** |
| `RESET_CONFIRM_TYPE` | `{word}를 입력하십시오` | **게이트 위반(2판 추가 — 1판 전수 표 누락, 세션 발견·검토자 확정)** — {word}=백엔드 고정 상수 "delete"(비번역 계약, confirmSpecs.tsx:216·main.py:575-576)라 "delete를"의 오독 위험은 사실상 없으나, D-4 게이트의 무예외 원칙(C7 교훈 — 예외 키 목록 금지)과 자기모순이므로 **문구 개정으로 해소**. **개정 D-2 ④** |
| `APPLY_ONE_OK` | `{profile}을(를)` | 병기형 — 오류는 아니나 표시("eGPU을(를)")가 거칠고, 불변 명사 문형으로 공짜 해소 가능. **개정 D-2 ③ — R10-3 채택 확정(세션 08-13)** |
| `SAVE_OK` | `{profile}에` | **유지** — "에"는 이형태 없는 불변 조사(어떤 이름에도 틀리지 않는다) |
| `DELETE_CONFIRM_TITLE`/`DELETE_OK` | `「{name}」의` | 유지 — "의" 불변 + 홑낫표가 이름을 격리(LOCKED 잠금 유지) |
| `DISCOVER_ADDED`·`DISCOVER_ADDED_HAS_BACKUPS` | `{name}을(를)`·`「{name}」을(를)` | 유지 — {name}(게임 이름) 계열의 기존 병기 관례. 문구 대개편은 범위 밖(§10-A 「」 관례 절과 같은 처분) |
| `APPLY_PROBLEM_SPECIFIC` | `{names}은(는)` | 유지 — 동상 |
| `NAME_EDIT_OK_NOTE` | `「{name}」(으)로` | 유지 — 병기형 + 홑낫표 |
| `QAM_ABOUT_GUIDE` | `프로필({dock}/{internal})을` | 유지 — 조사가 치환자가 아니라 머리명사 "프로필"에 걸린다(괄호 삽입구) |
| `RESTORE_*`·`BULK_*`·`SLOT_*`·`GAME_DETAIL_STATUS`·`CHECKIN_*`·기타 `{profile}` 사용 전 키 | `{profile} 프로필…` 등 | 유지 — 조사 비접촉 또는 불변 명사("프로필") 경유(권장 정본 문형) |

이 표 밖의 231키 전수에서 `}` 직후 이형태 단독 조사는 위 **3건**(+병기 1건)이 전부다(기계 grep으로 재확인 가능 — D-4 게이트가 그 grep의 상설판. 1판이 `RESET_CONFIRM_TYPE`을 놓친 것이 "손 전수는 새는다"의 실증이기도 하다).

### D-2. 개정 조문 전문 — §10-A 행 3개 추가

§10-A 표에 다음 행을 추가한다(값 = ko 바이트 확정, en은 §10-A가 확정하지 않는다 — ko만 잠금. 기존 관례):

| 키 | 신 값(ko) | 사유 |
|---|---|---|
| ① `APPLY_SUMMARY` | **"{profile} 프로필 적용 — 전체 {total}개 중 {applied}개 변경"** | D5 — 조사 없는 문형(§10-E). 부수 이득: "전환"은 A5 용어 계열("프로필 적용") 밖의 어휘였다 — 이번 개정으로 일괄 버튼(`BULK_APPLY` "{profile} 프로필 적용")과 결과 줄이 같은 동사를 쓴다(누른 것과 보고된 것의 어휘 일치). 명사구 헤드라인인 이유: 성패는 아이콘(D05)이 말하므로 "완료"류 단정을 문장이 반복하지 않는다 — 문제 행이 있는 결과에서도 이 헤드라인은 참이다 |
| ② `SAVE_GUIDE` | **"{dock} 연결 상태에서 게임의 그래픽 옵션을 맞춘 뒤 [{dock} 프로필 저장]을, {internal}일 때 쓸 그래픽 옵션을 맞춘 뒤 [{internal} 프로필 저장]을 누릅니다."** | D5 잠복 — "{dock}를"→"{dock} 연결 상태에서"(조사 소거), "{internal}으로 쓸"→"{internal}일 때 쓸"(계사 "일"은 이형태 없음 — 어떤 이름에도 성립). 두 "[… 프로필 저장]을"의 조사는 대괄호 버튼명 뒤라 비접촉 |
| ③ `APPLY_ONE_OK` | **"{profile} 프로필을 적용했습니다"** 【R10-3 채택 확정】 | 병기형 해소 — 조사가 불변 명사 "프로필"에 붙는 권장 정본 문형(§10-E ⓒ). `CHECKIN_ONE`("… {profile} 프로필에 …")과 문형 정합 |
| ④ `RESET_CONFIRM_TYPE` (2판 신설 — 지적 1) | **"확인하려면 아래에 다음 단어를 입력하십시오: {word}"** | D-1 ④ — 조사가 불변 명사 "단어"에 붙고 치환자는 문미로(§10-E ⓒ 변형). {word}가 라틴 고정어("delete")라 병기형("{word}을(를)")은 표기만 무거워져 불채택. 입력창 라벨("확인 입력")이 바로 아래라 문미 제시가 시선 흐름과도 정합 |

en 값: ①은 "Switched to {profile} — {applied} of {total} changed" 유지 가능하나 ko 어휘 정렬에 맞춰
"Applied {profile} profile — {applied} of {total} changed"를 **권고**(비잠금 — 구현 확정).
②③ en 무변경, ④ en 무변경("Type {word} below to confirm." — 조사 문제가 없는 언어).

### D-3. 개정 조문 전문 — §10-E 신설 (규칙 행)

> ### 10-E. 치환자 조사 규칙 (10판 신설 — D5의 구조 해소)
>
> 치환자(`{profile}`·`{dock}`·`{internal}`·`{name}`·`{names}` 등 실행 시 값이 결정되는 자리) **바로 뒤에
> 이형태 조사의 단독형을 붙이지 않는다**: 이/가 · 을/를 · 은/는 · 와/과 · 로/으로.
> 표시명은 사용자 지정 가능(F11)이고 게임 이름은 임의 문자열이라 **어떤 조사 선택도 언젠가 틀린다.**
> 받침 판정 코드를 넣는 안은 기각 — 라틴 문자·숫자·기호로 끝나는 이름에서 판정 자체가 성립하지
> 않고, 판정기는 "그 예외가 생기지 않는 구조"가 아니라 예외 처리의 증식이다.
>
> 허용 3형:
> ⓐ **불변 조사**(에·의·도·만·까지·부터·마다 등 — 이형태가 없어 어떤 이름에도 틀리지 않는다).
> ⓑ **병기형**("을(를)"류) — `{name}` 계열의 기존 관례로 **유지**하되 신규 문구에는 권장하지 않는다.
> ⓒ **불변 명사 경유**("{profile} 프로필을 …" — 조사가 '프로필'에 붙는다) — **권장 정본 문형**.
> 괄호·홑낫표가 치환자를 격리한 자리(`「{name}」의`, `({dock}/{internal})을`)는 조사가 치환자에
> 붙은 것이 아니므로 대상 밖이다.
>
> 기계 판정은 §15-B ⑪(D-4). 이 절은 어휘 규칙 행이다 — 값 고정 게이트(`test_wording_10a`)의 대상이
> 아니며, 소관 분리는 §10-A "값 고정 게이트와의 관계" 문단의 문법을 따른다.

### D-4. 검사 잠금 명세 (C3 규율: 이 초안의 설계 확정 → 검사 잠금 → 구현 순서)

1. **`test_wording_10a.py`**: `LOCKED`에 **D절 4행**(D-2 ①~④의 ko 바이트 그대로) + **E절 1행**
   (`RESET_ZONE_BODY` — E-1 ⑥)을 추가. **`TOTAL_FIXED` 28 → 33** — 상수 갱신은 의식적 1곳(파일 자체
   규율). ⚠ 산정 전제(2판 — 지적 4): `TOTAL_FIXED = len(LOCKED)+len(PINNED)`이고 **현재 PINNED는
   0(래칫 전부 합류)**이므로 "LOCKED 28→33"과 "TOTAL 33"이 같은 수다 — PINNED가 다시 생기는 날
   두 수는 갈라진다(개념을 뭉쳐 적지 말 것). 세션 통지의 "31"은 지적 1·14 신설 행 이전 값이며,
   §10-A 규칙("값을 명시한 키는 전부 두 표 중 하나에 — 면제 목록 금지")의 기계 적용으로 33이 된다.
2. **`qa/test_wording_particles.py` 신설**(§15-B ⑪): ko.json 전 키에 대해 `}` 직후
   `(로|으로|이|가|을|를|은|는|와|과)` 매치를 위반으로 본다. 단 매치가 병기형이면 허용(ⓑ) —
   허용쌍 = `을(를)`·`이(가)`·`은(는)`·`와(과)`·**`(으)로`**(2판 추가 — 지적 3: `(으)로`는 여는 괄호가
   선행하는 형태라 현행 실물(「{name}」(으)로)은 `}` 비인접 덕에 우회되지만, 홑낫표 없는
   `{x}(으)로`가 생기는 날 §10-E ⓑ 허용형을 게이트가 잡는 명세 구멍이었다). 불변 조사는 매치
   집합에 없으므로 자동 허용(ⓐ). en은 대상 아님. 음성 대조군: `APPLY_SUMMARY`에 "{profile}로"를
   되돌리면 FAIL임을 주석으로 명기. 기대 위반 수 = **0**(예외 목록 금지 — 걸리면 문구를 §10-E
   3형으로 고친다. §15-B ⑨의 "키 예외 0" 문법. `RESET_CONFIRM_TYPE`도 예외가 아니라 개정이다 — D-1 ④).

### D-5. 파급

| 대상 | 내용 |
|---|---|
| 코드 | `ko.json` 4값 개정(D-2 ①~④) · en 1값 권고 개정 · 소비처 코드 무변경(자리표시자 집합 불변 — `test_i18n_sets` 자동 통과. 검토자 주입 시뮬레이션 PASS 실측) |
| 잠금 | LOCKED — D절 몫 4행(총계는 E절 1행 합산 33, D-4 ①·PINNED=0 전제) · A5 게이트: 신 값 전부 금칙("미완"·프로필 의미 "설정") 무접촉 — "게임의 그래픽 옵션"은 허용 어구 |
| 검사 | `test_wording_10a`(표+상수) · `test_wording_particles` 신설 · 상태박스 프로브가 `APPLY_SUMMARY` 문구로 목을 만들었다면 목 문자열 갱신 |
| 타 절 | **§5-B 다이어그램의 SAVE_GUIDE 예시문 갱신**(D-2 ② 값으로) · §3-B ⑤(헤드라인 키 목록 — 키명 불변, 무변경) · §10-C의 SAVE_GUIDE "잠정" 표기는 §10-A 확정 행으로 승격(§10-C 행에 "→ §10-A(10판 확정)" 주석) |
| 실기 | §16-⑧(문구 체감)에 자동 포섭 — 별도 항목 불요 |

---

## E절. §7 확인창 중복 정리 (세션 확정 — 2026-08-13 범위 변경 통지로 조건부→확정 승격)

### E-0. 현행 조문 인용

§7:
> - 전체 초기화: `resetAll()` 1차 → 게이트 `ResetConfirmSpec`(입력형). **⚠ warnBlock**(A8):
>   > ⚠ 등록 {games}개와 프로필 {profiles}개를 지웁니다. 백업은 남지만, **되살리려면 그 게임을 다시 등록한 뒤 백업에서 복원해야 합니다.**
>   — **재등록 전제 명시**(F23, 세션 확정). … **이하 현행 본문(백업 잔존·표시명 소실·`MANAGE_KEEP_CONFIG`·type-to-confirm `delete`) 유지** + **제외 목록 고지**: …

실물(스샷 before-confirm-reset-0016 + `confirmSpecs.tsx:233-248`): warnBlock(수량+백업+재등록) 아래에
`RESET_CONFIRM_BODY`("등록 {games}개와 저장된 프로필 {profiles}개를 지웁니다." — **수량 2회째**)와
`RESET_CONFIRM_BACKUP_KEPT`("백업은 남습니다 — 방금 지운 프로필은 백업에서 되살릴 수 있습니다." —
**백업 잔존 2회째, 게다가 F23이 지운 '재등록 전제 없는 낙관' 문장의 잔존**)가 그대로 선다.
**중복은 구현 이탈이 아니라 §7 조문("warnBlock 신설 + 이하 현행 본문 유지")이 시킨 결과다.**

### E-1. 개정 조문 전문 (§7 해당 항 대체)

> - 전체 초기화: `resetAll()` 1차 → 게이트 `ResetConfirmSpec`(입력형). 본문 구성(위→아래 — **각 사실은
>   정확히 한 번만 말한다**, 10판):
>   1. **⚠ warnBlock**(A8 — 수량·백업 잔존·재등록 전제의 **유일한** 자리):
>      > ⚠ 등록 {games}개와 프로필 {profiles}개를 지웁니다. 백업은 남지만, **되살리려면 그 게임을 다시 등록한 뒤 백업에서 복원해야 합니다.**
>   2. `RESET_CONFIRM_NAMES`(직접 정한 표시 이름 {n}개 — `named>0`일 때만).
>   3. `RESET_CONFIRM_EXCLUDED`(감지 제외 목록 {n}건 — `excluded>0`일 때만).
>   4. `MANAGE_KEEP_CONFIG`(게임 설정 파일 원본 불가침) — **회색(META)으로 그린다**(R10-4 채택,
>      10판): 안심 정보가 경고색을 입으면 ⚠ warnBlock과 위험 신호가 둘이 된다. 이 키를 경고색으로
>      그리는 자리는 실물 2곳(`confirmSpecs.tsx:246` 초기화 · `:124` 등록 해제) — **키 단위 원칙**
>      ("안심 정보는 경고 옷을 입지 않는다")이므로 두 곳 모두 강등한다(같은 판단이 화면마다 갈리지
>      않게 — §18-① 논거와 동형).
>   5. `RESET_CONFIRM_TYPE` + type-to-confirm `delete` 입력(2단 방어 **계약** 불변 — 문구만 D-2 ④가 개정).
>   **`RESET_CONFIRM_BODY`·`RESET_CONFIRM_BACKUP_KEPT`는 키째 제거한다**(§10-B 10판 추가분) —
>   9판까지 warnBlock과 각각 수량·백업 잔존을 이중으로 말했고, BACKUP_KEPT는 F23이 걷어낸
>   "재등록 전제 없는 되살림 약속"을 문서 반대편에 되살려 두는 자리이기도 했다.
>   입력값 보존(GP#9)·활성 조건(D-01)·상태 분기표(F24)·2단 방어 계약 전부 불변.
>   6. **설정 화면 중복 정리(2판 편입 — 세션 채택, 검토자 발견)**: 확인창 밖 SettingsPopup 본문에서도
>      같은 사실이 두 번 선다 — `RESET_ZONE_BODY`("… 모두 지웁니다. **백업은 남습니다.**", :136)와
>      버튼 아래 `RESET_HINT`("**백업은 남고**, 게임 설정 파일 원본은 …", :138)가 동시 렌더.
>      "각 사실은 한 번만" 원칙을 설정 화면에도 적용해 **백업 잔존은 `RESET_HINT`가 전담**한다:
>      > `RESET_ZONE_BODY` 신 값(ko): **"전체 초기화 — 등록 {n}개와 저장된 프로필을 모두 지웁니다."**
>      > (en 동반 개정 권고·비잠금: "Full reset — deletes {n} registration(s) and every saved profile.")
>      `RESET_HINT` 무변경. 정본 §7 스케치의 해당 줄(:511 "…모두 지웁니다. 백업은 남습니다.")을 신 값으로
>      동기(:513 RESET_HINT 줄은 그대로). 이 행은 값 명시 행이므로 §10-A 표에 추가하고 LOCKED로
>      잠근다(D-4 ① 합산 — 총 33의 다섯째 행).

### E-2. 근거

- 중복 자체가 결함(신규 소견 ⓒ — 세션 확정 접수)인 것에 더해, **BACKUP_KEPT 잔존은 정보 정확성
  문제다**: F23 확정("백업은 남습니다"만으로는 거짓에 가깝다)과 정면 충돌하는 문장이 같은 화면에
  공존한다 — 지금 확인창은 재등록 전제를 말했다가(warn) 곧바로 무전제 되살림을 약속한다(KEPT).
- 파괴 규모 고지가 warnBlock 한 곳이 되는 것은 A8("확인 팝업에만 ⚠ 블록")의 원 취지와 정합 —
  같은 수를 두 톤으로 두 번 말하면 어느 쪽이 정본인지 화면이 답하지 못한다.

### E-3. 파급

| 대상 | 내용 |
|---|---|
| 코드 | `confirmSpecs.tsx` `makeResetConfirmSpec` body에서 두 줄 제거(:238-239) · `MANAGE_KEEP_CONFIG` 스타일 META 강등 2곳(:246·:124) · `SettingsPopup.tsx` 무변경(RESET_ZONE_BODY는 값만 개정) · `ko.json`/`en.json`에서 `RESET_CONFIRM_BODY`·`RESET_CONFIRM_BACKUP_KEPT` 키 제거 + `RESET_ZONE_BODY` 값 개정 |
| 주석 | **낡은 주석 2곳 동기 필수(2판 — 지적 13)**: ⓐ `confirmSpecs.tsx:236-237`("파괴 규모 고지는 입력이 아니라 **이 줄이** 전담") — 그 줄이 사라지므로 전담자를 warnBlock으로 고쳐 쓴다 ⓑ `main.py:574`(같은 문장 "…모달 본문의 파괴 내역(games·profiles) 표시가 전담한다") — **재량 처분 = 갱신**("…확인창 ⚠ warnBlock(games·profiles)이 전담한다"): 이 프로젝트에서 낡은 주석은 반복적으로 오진의 뿌리였고(§15-C E2류), 동작 무접촉 주석 1건이다. G절 "백엔드 0파일" 서술에 이 예외를 명기한다 |
| §10-B | **10판 추가 제거 2종** 등재(기존 "18종 — P15에서만"은 이력이므로 수 불변, 별도 배치로 추가): `RESET_CONFIRM_BODY` · `RESET_CONFIRM_BACKUP_KEPT`. 실물 키 수 **231→229**(2판 정정) · **정본 §10-B의 "실물 대조는 245→227키" 문장 자체가 낡았다(실물 231 — P16 이후 증분 미반영)**: 10판 정정 목록에 등재해 이 문장을 231 기준으로 고친다 — 안 고치면 다음 개정이 또 물려받는다(검토자 부수 관찰 ①) |
| 잠금 | 제거 2키는 비잠금(LOCKED 무접촉) · `RESET_ZONE_BODY` 신 값은 **LOCKED 편입**(D-4 ① 합산 — 총 33) · A5 무접촉 |
| 검사 | **`test_confirm_keys.py` 개수표** — reset spec 키 수 −2를 **의식적으로** 갱신 + **음성 대조군 하드코딩 갱신 필수(2판 — 지적 12)**: `:192-194`의 기대 문구 "makeResetConfirmSpec의 문구 키가 **9개다**" → "**7개다**"(안 고치면 FAIL 실측 — 등호 검사가 알려주는 것이 정상 동작이나, 지시 없이 만나면 원인 추적 비용이 든다) · `test_i18n_sets` 자동 적응 · `settings_popup_probe` — RESET_ZONE_BODY 신 값으로 목 갱신 |
| 타 절 | §10-B′(재사용 표의 "`RESET_CONFIRM_*` 전열" — "전열"에서 제거 2종 예외 각주) · §15-B ⑩(spec 선언↔구성 대조 — 선언 쪽도 함께 줄어 자동 정합) · §7 스케치 :511 동기(E-1 ⑥) |
| 실기 | §16-⑦(가상 키보드 경로 — 최우선 유지)·⑮ ⓕ 초기화 확인창 스샷 1장(중복 소멸 + KEEP_CONFIG 회색 확인) |

---

## F절. §4-C 추가 조항 — 확인창 입력면 시각 계약(ⓐ) + 비활성 Primary 권고(ⓑ)

세션 확정(2026-08-13): ⓐⓑ 모두 이번 세션에서 수정한다. 조문화 수준도 확정됐다 —
**R10-2 = 참모 추천대로 채택(세션, 2판): ⓐ = §4-C 시각 계약 조문화 / ⓑ = 권고 수준 + §16 실기 판정.**
채택된 비대칭의 근거 —
- ⓐ의 렌더 수단은 전부 **우리 코드 안**이다(`popup.tsx` `specBody`의 TextField — 두 렌더러가 같은
  본문을 그린다는 §4-C 기존 계약의 연장선). 계약을 박아도 구현 불능 위험이 없고, 박지 않으면 다음
  구현자가 흰 기본값을 "정상"으로 오인할 근거가 남는다(busy 투명도 0.4를 §3-B에 박아 둔 것과 동급).
- ⓑ의 실물 버튼은 **중첩 모드에서 Steam `ConfirmModal` 내부**다 — 우리 스타일 손이 닿는지 미실측이고,
  "실측 없음 ≠ 불가"이므로 **해 보고 판정할 사안**이지 바이트 계약으로 선약속할 사안이 아니다.
  계약으로 박았다가 수단이 없으면 계약이 거짓이 된다.

### F-1. 개정 조문 전문 — §4-C 말미에 추가

> - **입력형 확인창의 입력면 시각 계약(10판 — 실기 신규 소견 ⓐ)**: TextField의 입력면은 **주변
>   다이얼로그 배경과 같은 어두운 계열**이어야 하며(흰 통짜 면 금지 — Steam 기본값이 어두운
>   다이얼로그에서 이질적으로 튀는 것이 실측 결함), 경계는 미세 대비 또는 1px 윤곽으로 식별된다.
>   글자색은 그 면 위에서 읽히는 밝은 계열. **두 렌더러(중첩·폴백)가 같은 시각을 그린다** — 렌더
>   자리가 `specBody` 한 곳이므로 이 계약도 그 한 곳에 산다. 구체 수치·수단(TextField의 style 계열
>   prop 또는 래퍼 컨테이너 CSS)은 구현 재량이고, 판정은 실기 스샷(§16-⑮ ⓖ)이다.

### F-2. ⓑ 권고 (조문 아님 — 구현 지침)

증상: type-to-confirm 미완 상태의 [전체 초기화](비활성 Primary)가 **버튼 면 없이 흐린 글자만**으로
렌더된다(스샷 확인 — 옆 [취소]는 면이 있다). "여기 버튼이 있고 지금은 잠겨 있다"가 아니라 "죽은
텍스트"로 읽힌다.
- 수단 후보(중첩 모드): ① `strOKButtonText`에 ReactNode를 줄 수 있는지 실기 확인 — 가능하면
  `gate.okDisabled` 조건부로 면(배경 pill)을 가진 span 래핑 ② 불가능하면 중첩 모드는 현상 유지.
- 폴백 렌더러(우리 소유)는 무조건 정리: 비활성 시에도 면 유지 + 투명도 강하(disabled 기본 스타일에
  기대지 않는다).
- 판정: §16-⑮ ⓗ. ①이 불가로 판명되면 **알려진 한계로 §15-D에 1행 등재**(중첩 모드 비활성 Primary
  시각은 Steam 소유 — 병적이 아니라 관습 수용)하고 종결한다.

### F-3. 파급

| 대상 | 내용 |
|---|---|
| 코드 | `popup.tsx` `specBody`(TextField 시각) · `ConfirmOverlay` 버튼 비활성 시각 · (실기 판정부) `SpecConfirmModal` okText 래핑 |
| 잠금·검사 | 문구·키 무접촉. `popup_shell_probe` ③(양 모드 동일 spec 렌더)이 스타일까지 대조한다면 목 갱신 |
| §16 | **⑮ 신설(10판)**: 표시층 수리 스샷 표본 — ⓐ 팝업 G 목록 ⓑ 팝업 G 상세(+세로 잘림 재실측) ⓒ QAM 최상단(아이콘 in-button) ⓓ 세로 스택 버튼 좌측 정렬 축 ⓔ 상태박스 카드형(268px 줄바꿈) ⓕ 초기화 확인창(중복 소멸) ⓖ 입력면 시각 ⓗ 비활성 Primary 수단 판정. 전수 금지 — 표본만, 세션 직접 캡처(HANDOFF-UI-FIX §2-4 규율) |

---

## G절. 총괄 파급표 (잠금·검사·회귀 — 구현 워커 체크리스트)

**잠금 3계 최종 상태**:
| 잠금 | 이번 개정 후 |
|---|---|
| `test_wording_10a` LOCKED | 28 → **33**(D절 4행 + E절 1행 — 2판 집계) — `TOTAL_FIXED`=33 동기 갱신(PINNED=0 전제, D-4 ①) |
| A5 전역(`test_wording_a5`, 462건 기준) | 위반 순증 0 — 신 값 전부 허용 어구 검증 완료(D-5). 키 −2로 모수만 감소 |
| E1 게이트(`build.sh` grep ③) | 무접촉 — B절(BulkApplyButton 아이콘)·C절(마커 줄 이동)이 닿지만 문법 불변. **grep ⑤(DialogButton 단일 관문) 신설**(A절) |
| 엔진 fence(`engine_allowed.diff`) | **무접촉**(이번 개정에 백엔드 동작 변경 0 — main.py **주석** 1건만 동기, E-3 지적 13) |

**회귀 35종 중 영향 테스트**(그 외는 무접촉 — 백엔드 계약군 전체 무영향):
| 테스트 | 영향 | 조치 |
|---|---|---|
| `test_wording_10a` | D·E절 | 표 5행 + TOTAL_FIXED=33 |
| `test_wording_particles`(신설) | D절 | §15-B ⑪ 등재 |
| `test_wording_a5` | E절 | 키 제거 2종 — 자동 적응 확인 |
| `test_i18n_sets` | D·E절 | 자동 적응(집합 기반) — 단계 종료 시 통과 확인 |
| `test_confirm_keys` | E절 | reset spec 개수표 −2 + **음성 대조군 하드코딩 "9개다"→"7개다"(:192-194)** 의식적 갱신 |
| `test_qam`(qam_probe) | B·C절 | icon 판독 children화(:218·:309·:311) · 상태박스 **border 셀렉터(:183) → 카드 셀렉터**(테두리 폐기 후 필수) |
| `test_frontend_e1`(frontend_probe) | **B절**(2판 정정 — C절 아님: statusBox 판독 0건) | **무수정-통과** — frontend_probe.cjs:229의 BULK_APPLY textOf 선별이 아이콘 span 선두 삽입에도 성립(textOf가 요소 서브트리 "" 반환 — 검토자 실측). 근거를 남기는 이유: 통과가 우연으로 읽히면 다음 개정이 이 프로브를 잘못 신뢰하거나 잘못 의심한다 |
| `test_games_popup`·`test_settings_popup`(각 프로브) | A절 | PopupButton 통과 후 스타일 목 갱신 — 판정 항목 불변 |
| `test_discover_ui`(discover_probe) | A절 | 스타일 목 갱신 + **음성 대조군 정규식 3식 재작성**(:495·:498·:525 — 소스 텍스트에서 `<DialogButton`을 매칭하므로 `PopupButton` 치환과 함께 죽는다) |
| `test_restore_ui`(restore_probe) | A절(2판 추가) | restore_probe.cjs:154 — discover와 동일 계열 경로, 치환 후 갱신 확인 |
| `probe_kit.cjs` | A절 | **무수정** — 근거: 래퍼가 children을 JSX 자식으로 전달하는 확정형(A-2 1항)이라 :172(children prop 스킵)·:208(JSX 자식 라벨 판독)이 현행 그대로 성립한다. prop 전달형이었다면 회귀 4종 파괴(실측) |
| `test_popup_shell`(popup_shell_probe) | A·F절 | 양 모드 동일성 검사에 시각 대조 있으면 갱신 |
| `build.sh` | A절 | grep ⑤ 추가 |

**코드 접촉 파일 전량**: `popup.tsx` · `index.tsx` · `BulkApplyButton.tsx` · `GamesPopup.tsx` ·
`DiscoverPopup.tsx` · `SettingsPopup.tsx` · `confirmSpecs.tsx` · `i18n/ko.json` · `i18n/en.json` ·
`qa/*`(위 표) · `build.sh`. **백엔드 동작 변경 0파일**(예외 1건 명기 — `main.py:574` **주석만** 동기,
E-3 지적 13. 엔진 fence 5파일과 무관하고 diff 게이트 비대상).

**착수 순서 권고**(진단 §6 승계 + 이 초안 반영): ① B절(확정·2곳·위험 0) → ② D절(i18n+게이트 —
검사 먼저 잠그고 값 반영, C3 순서) → ③ E절 → ④ A절(폭 규약 — 치환 범위가 가장 넓다) →
⑤ C절(B안 확정 — 즉시 착수 가능) → ⑥ F절 → 빌드·재설치·스샷 표본(§16-⑮) → 회귀 전량 → qa-lead 경량
게이트 + Codex 이종 1회(브리핑에 "Steam 본체 CSS 계열은 미니 프로브가 원리적으로 못 잡는다" 명기).

---

## H절. §19 개정 이력 — 10판 항 초안 + 제목줄 정정

제목줄: `… (DESIGN-UX-2026-08-11) — 개정 8판` → `… — 개정 10판` (8→9 누락 흡수, 판수 정본은 §19).

> - **개정 10판 (2026-08-13, 참모 초안 → 세션 채택)** — Game Mode 실기 1차 표시층 결함 D1~D6
>   (`DIAG-UI-FIELD-2026-08-12.md`, 단 §2 일부는 후속 실측으로 정정) 반영. 백엔드·엔진 무접촉.
>   1. **§4-G 신설 — 팝업 버튼 폭 규약**(D1): Steam `width:100%` 오염은 flex 자식에도 발현(진단 §2
>      반증 실측 584.7px·이름 0px 붕괴) → `PopupButton` 래퍼 단일 문 + `width:fit-content` +
>      build.sh grep ⑤. 세로 스택 좌측 정렬 축. §5-A 110px 계약은 불변(현 상태가 위반이었다).
>      확인창 2종 푸터는 면역 실측 — 제외.
>   2. **§3-A 정오**(D2·D6): `ButtonItem.icon`은 Field 라벨 슬롯(버튼 위 26px 고아·16×20 왜곡) —
>      사용 금지, children 안 `ICON_SLOT_STYLE` 관용구로 통일 + 상수 popup.tsx 승격(3파일 복제 소거).
>   3. **§3-B 카드형 재작성**(D3·D4, **사용자 채택: B안** 2026-08-13): 테두리 박스 → CARD_STYLE 결과
>      카드 2장(간격의 문 = 컨테이너 gap 8px 하나, marginBottom 0 override), 슬롯·수명·색 계약 전량
>      승계, "고정폭과 가변 텍스트를 같은 flex 행에 두지 않는다" 폭 규약으로 D4 원천 봉쇄. 시각
>      표기는 머리 행 우측 정렬로 **개정**. 정오 병기: 9판 ⑤의 헤드라인 키 2종 표기는 실물 3종
>      (`APPLY_ALL_STOPPED` 포함, §10-C 기등재)의 누락이었다.
>   4. **§10-A 4행 + §10-E 신설**(D5): `{placeholder}` 직후 이형태 단독 조사 금지(전수 점검 — 위반
>      실물 3건: `APPLY_SUMMARY`·`SAVE_GUIDE`·`RESET_CONFIRM_TYPE`), 조사 없는 문형으로 개정
>      (+`APPLY_ONE_OK` 병기형도 불변 명사 문형으로), `test_wording_particles` 게이트 신설(병기
>      허용쌍에 `(으)로` 포함), **LOCKED 28→33·TOTAL_FIXED 33**(§7 몫 `RESET_ZONE_BODY` 합산,
>      PINNED=0 전제).
>   5. **§7·설정 화면 중복 정리**: warnBlock이 수량·백업·재등록 전제를 전담 — `RESET_CONFIRM_BODY`·
>      `RESET_CONFIRM_BACKUP_KEPT` 키 제거(§10-B 10판 추가 2종). F23과 충돌하던 무전제 되살림
>      약속 소멸. 설정 화면도 동일 원칙 — `RESET_ZONE_BODY`에서 "백업은 남습니다." 제거
>      (`RESET_HINT` 전담). `MANAGE_KEEP_CONFIG`는 안심 정보이므로 경고색 → 회색 강등(2곳).
>      **§10-B 정오 병기**: "실물 대조는 245→227키" 문장은 낡았다(P16 이후 실물 231) — 231→229로
>      정정.
>   6. **§4-C 입력면 시각 계약**(실기 소견 ⓐ) + 비활성 Primary는 권고·실기 판정(ⓑ — 수단이 Steam
>      소유일 수 있어 계약화 보류). §16-⑮ 스샷 표본 신설.

---

## I절. 검토·기각 기록 (이 초안에서 되살리지 않은 것)

| 후보 | 판정 |
|---|---|
| Framegen식 파괴 동작 표기(평범한 면 + **빨간 굵은 글씨**) | **기각 확정**(세션 2026-08-13) — A8(색 2단·빨강 없음)·§18-①(빨간 구획 제거) 정면 충돌. 채택 후보로 올리지 않는다 |
| `STACKED_BUTTON_STYLE` 국소 상수 4곳(진단 §3 원안) | **대체** — 실측이 "flex 자식도 오염"을 밝혀 국소안은 미봉(세션 기확정). §4-G 전역 규약이 승계 |
| 상태박스 C안(진짜 로그 이력) | **재상정 없음** — 유지보수 모드(신규 개발 닫힘) 충돌로 기왕 후순위. 이 초안은 언급만 하고 조문에 되살리지 않았다(C-1 정직성 조항) |
| 받침 판정(한글 종성 검사) 조사 자동 선택 | **기각** — §10-E 본문(라틴·숫자·기호 이름에서 판정 불능, 예외 증식 구조) |
| `popupWidth` 재도입(세로 잘림 대응) | **기각 유지** — A10 무죄 재확인(A-1: 잘림 주범은 라벨 붕괴, 스크롤러 실측 도달) |

---

## 부록. 신설·개정 값 요약 (구현 워커 복붙용)

```jsonc
// ko.json — 값 개정 5건 (LOCKED 편입 — 검토자 주입 시뮬레이션 관련 게이트 PASS 실측)
"APPLY_SUMMARY": "{profile} 프로필 적용 — 전체 {total}개 중 {applied}개 변경",
"SAVE_GUIDE": "{dock} 연결 상태에서 게임의 그래픽 옵션을 맞춘 뒤 [{dock} 프로필 저장]을, {internal}일 때 쓸 그래픽 옵션을 맞춘 뒤 [{internal} 프로필 저장]을 누릅니다.",
"APPLY_ONE_OK": "{profile} 프로필을 적용했습니다",
"RESET_CONFIRM_TYPE": "확인하려면 아래에 다음 단어를 입력하십시오: {word}",
"RESET_ZONE_BODY": "전체 초기화 — 등록 {n}개와 저장된 프로필을 모두 지웁니다.",
// en.json — 권고 2건(비잠금)
"APPLY_SUMMARY": "Applied {profile} profile — {applied} of {total} changed",
"RESET_ZONE_BODY": "Full reset — deletes {n} registration(s) and every saved profile.",
// 키 제거 2건 (ko/en 동반): RESET_CONFIRM_BODY, RESET_CONFIRM_BACKUP_KEPT
```

검사: `test_wording_10a` LOCKED += {APPLY_SUMMARY, SAVE_GUIDE, APPLY_ONE_OK, RESET_CONFIRM_TYPE,
RESET_ZONE_BODY}(ko), **TOTAL_FIXED=33**(PINNED=0 전제) · `test_wording_particles` 신설(병기 허용쌍
`(으)로` 포함) · `test_confirm_keys` reset 개수 −2 + 음성 대조군 "9개다"→"7개다"(:192-194) ·
`build.sh` grep ⑤(DialogButton 단일 관문) · 주석 동기 2곳(confirmSpecs:236-237 · main.py:574) ·
프로브 갱신 = G절 표. 래퍼 확정형(children 구조분해 + `ComponentProps<typeof DialogButton>`) = A-2 1항.
