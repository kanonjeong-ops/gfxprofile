/**
 * 백업 링의 한계를 사용자에게 말할 때 쓰는 **단 하나의 숫자**.
 *
 * ★ 왜 상수로 빼는가: 이 값은 화면 문구(`SAVE_CONFIRM_BACKUP_LIMIT`)와 회귀 테스트
 *   (`qa/test_restore_path.py`)가 **같이** 쓴다. 문구에 숫자를 박아 두면 둘이 조용히 어긋나고,
 *   어긋난 쪽이 화면이면 **사용자에게 거짓 약속을 하게 된다** — 실제로 그랬다(2026-08-07 QA 반려 ①:
 *   "10번"이라고 적었는데 실사용 실측은 6번이었다. 위험을 2배 낙관한 것이다).
 *
 * ★ 왜 이 숫자인가: `disk` 백업과 `profile_*` 대피본이 **`BACKUP_KEEP`(10) 한 링을 공유**한다.
 *   적용 1회가 링에 미는 개수는 사용 방식에 따라 다르다(실측):
 *     · 게임을 안 띄우고 적용만 반복  → 1개/회  → **10회**에 대피본 소멸
 *     · 게임이 설정을 다시 쓴 뒤 적용 → 2개/회  → **6회**에 소멸  ← 실사용은 이쪽이다
 *       (G3 체크인이 직전 프로필을 한 번 더 백업한다 — `engine.py:501-504`)
 *   **낙관하지 않는 쪽을 고른다.** 실측 최소(6)보다 작은 값이라야 경고가 일찍 뜬다.
 */
export const BACKUP_WARN_APPLIES = 5;

/**
 * 전체 화면 스크롤 영역의 **하단 여백**(F2 — 사용자 실기로 재현 확정된 버그).
 *
 * 증상: 목록을 끝까지 내려도 **마지막 항목이 Steam 하단바(footer)에 가려** 보이지 않는다.
 * 원인: route 화면은 하단바 위에 그려지고, 그 높이를 우리가 비워 주지 않으면 스크롤 끝이
 * 하단바 뒤로 들어간다 — 상단을 `56px` 비워 준 것(제목이 상단바에 가려졌던 문제)과 **같은 뿌리**다.
 *
 * ★ 왜 상수인가: 「현황」·「게임 추가」·「관리」가 **한 스크롤 컨테이너**를 공유하므로
 *   값이 사는 자리도 하나여야 한다(설계 DELETE §4-B의 "F2 수정과 같은 값 공유").
 *   탭마다 자기 패딩을 두면 새 탭이 생길 때마다 하나씩 빠뜨리게 되고, 빠뜨린 탭에서 F2가
 *   그대로 재발한다. **적용 지점은 `StatusPage`의 스크롤 컨테이너 한 곳뿐이다** —
 *   탭 컴포넌트는 자기 하단 패딩을 따로 두지 않는다(두면 여백이 두 번 붙는다).
 *
 * ⚠️ 정확한 하단바 높이는 Steam 내부 값이라 우리가 읽을 수 없다. 넉넉하게 잡는다 —
 *   조금 남는 여백은 보기에 어색할 뿐이지만, 모자란 여백은 **항목을 통째로 가린다.**
 *   (실기에서 아직도 가려지면 이 숫자만 올린다. 고칠 자리가 하나라는 것이 이 상수의 값어치다.)
 */
export const LIST_BOTTOM_PADDING = "80px";

/**
 * 프로필 **표시명 길이 상한**(F11 ①) — 화면 안내 문구가 쓰는 숫자.
 *
 * ★ **정본은 백엔드 `gfxp/labels.py`의 `MAX_LEN`이다.** 여기 값은 그것을 화면에 말하기 위한
 *   사본이고, `qa/test_profile_names.py`가 **두 값이 같은지 직접 대조**한다 —
 *   어긋나면 FAIL이다. 숫자를 문구에 다시 박지 마라(2026-08-07 QA 반려 ①의 형태:
 *   화면이 실측과 다른 수를 약속했고, 그 약속이 거짓이었다).
 */
export const PROFILE_NAME_MAX = 20;
