/**
 * 백업 링의 한계를 사용자에게 말할 때 쓰는 **단 하나의 숫자**.
 *
 * ★ 왜 상수로 빼는가: 이 값은 화면 문구(`SAVE_CONFIRM_BACKUP_LIMIT`)와 회귀 테스트
 *   (`qa/test_restore_path.py`)가 **같이** 쓴다. 문구에 숫자를 박아 두면 둘이 조용히 어긋나고,
 *   어긋난 쪽이 화면이면 **사용자에게 거짓 약속을 하게 된다** — 실제로 그랬다(2026-08-07 QA 반려 ①:
 *   "10번"이라고 적었는데 실사용 실측은 6번이었다. 위험을 2배 낙관한 것이다).
 *
 * ★ 왜 이 숫자인가: `disk` 백업과 `profile_*` 대피본이 **`BACKUP_KEEP`(10) 한 링을 공유**한다.
 *   적용 1회가 링에 미는 개수는 사용 방식에 따라 다르다(13판 실측 — `qa/test_restore_path.py`):
 *     · 게임을 안 띄우고 적용만 반복  → **0개/회** → 대피본이 밀리지 않는다
 *       (13판 §14-B: 디스크가 어느 슬롯과도 다를 때만 백업한다. A↔B 왕복은 언제나 어느 한쪽과
 *        같으므로 링을 태우지 않는다.)
 *     · 게임이 설정을 다시 쓴 뒤 적용 → 1개/회  → **10회**에 소멸  ← 실사용은 이쪽이다
 *   **낙관하지 않는 쪽을 고른다.** 실측 최소(10)보다 작은 값이라야 경고가 일찍 뜬다.
 *   ⚠️ 12판까지의 근거였던 *"G3 체크인이 한 번 더 백업한다"*는 **소멸했다**(체크인 제거 — §14-A).
 *     그때의 실측은 실사용 6회였고, 지금 값 5는 그 시절 기준으로도 새 기준으로도 보수적이다 —
 *     숫자를 올리려면 실측을 다시 하고 이 주석을 같이 고칠 것.
 */
export const BACKUP_WARN_APPLIES = 5;

/**
 * 팝업 안 목록 컨테이너의 **최대 높이**(§4-D).
 *
 * ★ 왜 px인가: 선례(lsfg-vk dist:1556-1561)가 px만 쓰고 vh는 **0건**이다. 두 방식 다
 *   「가정」인 것은 같으므로, 실물로 검증된 쪽을 따르고 조정은 이 상수 하나로 한다.
 * ★ 이 값은 **실기 실측(§16-⑥) 전의 초기값**이다 — 팝업 3종 각각 + 최대 상태(20행+note 2줄)
 *   에서 재보고 고칠 자리가 여기 하나라는 것이 이 상수의 값어치다.
 * ⚠️ 모달 자체 크기는 지정하지 않는다(A10) — 높이 제한은 **목록 컨테이너에만** 건다.
 */
export const POPUP_LIST_MAX_HEIGHT = "420px";

/**
 * 목록 컨테이너 **하단 미리보기 여백**(§4-D · GP#15).
 *
 * 포커스 행이 컨테이너 아래 경계에 딱 붙으면 **다음 행이 한 줄도 안 보여** 목록이 끝난 것처럼
 * 읽힌다. 그 완충이다.
 * ★ 전체 화면 시절의 하단 여백(80px — Steam 하단바에 가리는 것을 피하던 값)과는 **목적이
 *   다르다**. 그 값은 화면과 함께 사라졌고(P15), 이쪽은 스크롤 컨테이너 안쪽의 시각 완충이다.
 *   목적이 다른 두 값을 하나로 합치면 한쪽 근거가 바뀔 때 다른 쪽이 조용히 따라 움직인다.
 */
export const POPUP_LIST_TAIL_PAD = "48px";

/**
 * 목록 컨테이너의 **포커스 스크롤 여유**(§4-D · 실기 결함 2026-08-15 ② — 실측으로 확정).
 *
 * ★★ 무엇이 문제였나(숫자): 패드로 목록 맨 아래까지 내려가면 Steam은 **포커스 요소**(행 안의
 *   버튼)의 하단을 컨테이너 하단에 맞추고 멈춘다 — 실측에서 포커스 하단 455 = 컨테이너 가시
 *   하단 455로 **정확히** 일치했다. 그런데 카드 하단은 484라 **29px(= 메타 줄)이 잘렸다.**
 *   스크롤 여유는 81px(=239−158) 남아 있는데 쓰지 않는다.
 * ★★ **`POPUP_LIST_TAIL_PAD`를 늘리는 처방은 원리적으로 무효다**(이번 실측의 결론). 그 완충은
 *   **마지막 카드 아래**에 있어서, 「포커스 요소 하단을 맞춘다」는 계산에 애초에 들어가지 않는다.
 *   실제로 48px이 이미 있는 상태에서 잘렸다.
 * ★ 대신 **`scroll-padding-bottom`**을 쓴다 — 스크롤이 *멈추는 지점*에 여유를 요구하는 속성이라
 *   기전에 정확히 대응한다. 라이브 프로브 실측: 40px에서 scrollTop 158→198로 더 내려가고
 *   마지막 카드 하단 482 ≤ 가시 하단 493이 되어 **11px 여유**로 온전히 보였다.
 * ★ `POPUP_LIST_TAIL_PAD`와 **합치지 않는다**: 저쪽은 컨테이너 안쪽의 시각 완충이고 이쪽은
 *   포커스 스크롤이 남겨야 할 여유다. 목적이 다른 값을 한 상수로 묶으면 한쪽 근거가 바뀔 때
 *   다른 쪽이 조용히 따라 움직인다.
 * ⚠️ 값은 **카드의 「버튼 줄 아래에 남는 부분」**(메타 줄 + 카드 하단 패딩 ≈ 29px)을 덮으면 된다.
 *   40px은 그 실측값에 여유를 얹은 것이다 — 카드 구조가 바뀌면 이 숫자의 근거도 다시 재야 한다.
 */
export const POPUP_LIST_FOCUS_SLACK = "40px";

/**
 * 프로필 **표시명 길이 상한**(F11 ①) — 화면 안내 문구가 쓰는 숫자.
 *
 * ★ **정본은 백엔드 `gfxp/labels.py`의 `MAX_LEN`이다.** 여기 값은 그것을 화면에 말하기 위한
 *   사본이다. 숫자를 문구에 다시 박지 마라(2026-08-07 QA 반려 ①의 형태: 화면이 실측과 다른
 *   수를 약속했고, 그 약속이 거짓이었다).
 * ⚠️ **두 값을 대조하던 검사(`qa/test_profile_names.py`)는 삭제됐다**(2026-08-15 UI 검사 계열
 *   정리 — 사용자 지정). 지금 이 사본이 정본과 같다는 것을 **강제하는 것은 아무것도 없다.
 *   손으로 맞춰야 한다** — `labels.MAX_LEN`을 고치는 사람은 이 줄도 같이 고쳐야 하고,
 *   빠뜨리면 화면이 백엔드가 실제로 자르는 길이와 **다른 수를 약속한다.**
 *   (지금 두 값은 일치한다: `PROFILE_NAME_MAX = 20` == `labels.MAX_LEN = 20`.)
 */
export const PROFILE_NAME_MAX = 20;
