// 프론트가 쓰는 플러그인 버전 문자열은 여기 하나다 — 다른 파일에 리터럴로 다시 적지 마라.
// 값은 package.json과 두 사본이다. `build.sh bump`는 두 치환을 먼저 검증한 뒤 순서대로 쓴다.
// 테스트는 첫 매치를 대조하고, bump는 파일 전체 적중이 정확히 하나인지 요구한다. 같은 대입 형태를
// 주석에 쓰지 마라.
export const PLUGIN_VERSION = "0.1.0";
