# gfxprofile — 작업 규약

## 커밋 메시지

**제목 1줄 + 본문 1~5줄.** 분량은 내용이 정한다 — 한 줄로 끝나면 한 줄로 끝낸다.

- **제목** — 무엇을 건드렸는지. `—` 부제와 격언조를 붙이지 않는다.
  (✗ `한 동작 안에서는 쓴 순서가 진실이다 — QA DEFECT-01 처방 ③`
   ○ `백업 순서를 쓴 순서 기준으로 (DEFECT-01)`)
- **본문** — 왜 바꿨나, 되돌릴 때 알아야 할 것. 티켓 번호는 괄호로 짧게.
- **더 길어도 되는 때** — 데이터 형식 변경 · 삭제 · 동작 계약 변경처럼
  나중에 "그때 왜 그랬지"가 실제로 필요해지는 것. 그때는 길이를 아끼지 않는다.

**구구절절을 뺀다**: 소제목(`## 왜`) · 갈래·세계 전수 열거 · 중첩 목록 ·
★⚠️·굵게 남발 · 같은 사실을 여러 번 다르게 말하기.

**설명이 길어지면 코드 주석으로 옮긴다.** 주석은 코드 옆에 남아 그 함수를 여는
사람이 읽지만, 커밋 메시지는 일부러 `git log`를 파야 나온다. 길어지고 있다는 것은
주석에 적을 내용을 여기 적고 있다는 신호다.
